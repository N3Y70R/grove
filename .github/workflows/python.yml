name: python

on:
  push:
    paths:
      - "python/**"
      - ".github/workflows/python.yml"
  pull_request:
    paths:
      - "python/**"
      - ".github/workflows/python.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install (with test extra)
        run: pip install "./python[test]"

      - name: Unit + integration tests
        run: pytest python -q

      - name: Smoke test (--version / --help)
        run: |
          gwt --version
          gwt --help

      - name: Functional smoke (setup + list + create)
        run: |
          set -e
          git config --global user.email ci@example.com
          git config --global user.name ci
          tmp="$(mktemp -d)"; cd "$tmp"
          git init -q --bare origin.git
          git clone -q origin.git seed && cd seed
          git checkout -q -b main && echo v > app.txt && git add . && git commit -qm init
          git push -q origin main && cd ..
          gwt setup "file://$tmp/origin.git" --name repo
          cd repo
          gwt list --json
          gwt create PROJ-1 feature "ci smoke" --json
